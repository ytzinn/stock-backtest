; AUDIT Pass 0C — integration 마커 등록 (테스트 인프라, 프로덕션 코드 아님)
;   pytest -m "not integration"   → fast suite (DB 미접속, 모든 커밋)
;   pytest -m integration         → 합성 PostgreSQL 필요 (포트 5434+, PR 게이트)
[pytest]
markers =
    integration: 합성 PostgreSQL(포트 5434 이상)이 필요한 테스트. 운영 DB(5433) 접속 금지.

# SPEC_15 `S-4`(본 측정) + `S-6`(보고) — 공식 실행 기록

> **이 파일은 날짜 붙은 실행 기록이다. 수치를 고쳐쓰지 않는다** (CLAUDE.md 문서 정정 규칙).
>
> **사전등록**: `docs/설계/SPEC_15_cross_sectional_estimator_v0.4.md` (`c369401`)
> **선행 산출**: `A-0`·`A-1`(`f489051`) · `S-1`·`S-2`(`2822757`) · `S-3`·`S-5b`(`eb8d66c`)
> **표기 규칙**: `[검증된 사실]` / `[Claude 의견]` / `[확실하지 않은 사실]`. **수치도 라벨 대상.**

---

## §1. 착수 전 확정 — **`estimate.py` 실행 전 커밋**

> 이 절은 **결과를 보기 전에** 확정된 것이다. 이 커밋이 순서의 증거다.
> 결과를 본 뒤에 이 절의 어떤 항목도 바꾸지 않는다.

### 1-1. 판정 분기의 사전 인스턴스화

`[검증된 사실]` `A-2` 산출물(`a2_power.json`)의 값:

```
MDE_ICIR = 0.0806852984010316
IC*_lit  = 0.03        (진단 경계, 문헌 하단)
IC*_econ = 0.5/√26 ≈ 0.0981   (판정 경계, IR_target 0.5)

→  IC*_lit  <  MDE_ICIR  <  IC*_econ
```

따라서 v0.4 §7-3 조합표에서 **도달 가능한 행은 셋뿐**이고, 지금 확정할 수 있다.
결과가 나오면 아래 표의 해당 행을 **문구 그대로** 인용한다. 새로 쓰지 않는다.

| E1 결과 | 인용할 §7-3 행 | 다음 행동 (그대로 인용) |
|---|---|---|
| **PASS** (permutation 단측 p<0.05 且 부호가 가설 방향) | PASS 행 | 「예측력은 있으나 **breadth 26으로는 포트폴리오 수준에서 검출되지 않는다**(§9-3). G1 FAIL과 모순 아님. 채택 **여전히 보류.** breadth 축이 후속 SPEC 대상」 |
| **FAIL** | **중간 구간 행** (`IC*_lit ≤ MDE_ICIR < IC*_econ`) | 「경제적 유의 수준(IR 0.5)은 배제됐으나 **문헌 수준(0.03~0.05)은 판정 불가.** 확립된 것은 *현행 breadth에서 채택 근거 없음*이지 신호 부재가 아니다. **breadth 축 SPEC이 선행되어야 하며, 그 전에는 1/PBR 폐기 결정을 내리지 않는다**」 |
| **반대 부호 유의** | 역방향 행 | 「신호가 역방향. 별도 SPEC. **부호 뒤집어 재사용 금지**」 |

> **도달 불가능한 행을 인용하지 않는다.** `MDE_ICIR < IC*_lit` 의
> 「문헌 전형 수준의 신호조차 없다 → 1/PBR 랭킹 근거 상실」 행은 이 표본에서
> **성립할 수 없다** — `MDE_ICIR` 이 이미 `IC*_lit` 보다 크기 때문이다.
>
> `[Claude 의견]` v0.4 가 FAIL 행을 셋으로 분해해 둔 것이 여기서 값을 발휘한다.
> **FAIL 이어도 결론이 이미 확정돼 있어 `S-6` 에 해석의 여지가 없다.**
> 단일 경계였다면 지금 "근거 상실" 과 "판정 불가" 사이에서 고르고 있었을 것이다.

### 1-2. 선행 게이트를 `ran` 집합까지 조인다

`[검증된 사실]` 직전 세션에서 `controls.all_pass` 가 fail-open 이었다 —
`--skip-db` 로 `N-2` 를 건너뛰고도 `true` 였다. 고쳐졌지만 현재 구조는
**`controls.py` 가 `ran` 을 정직하게 채운다는 것을 신뢰**한다.

기대 집합을 **소비처(`estimate.py`)** 에 두면 그 신뢰가 검사로 바뀐다.

```python
EXPECTED_RAN = {'N1', 'N2a', 'N2b1', 'P1', 'P2'}
```

세 게이트 전부 fail-closed:
1. `controls['all_pass'] is True`
2. `set(controls['ran']) == EXPECTED_RAN`
3. `a2_power.json` 에 §6-1 금지 키 부재

**음성 대조**: `ran` 에서 원소 하나를 빼고 호출 → 거부되는지 확인 후 원복.

### 1-3. `[등재]` `N-2b-1` 공허 사건 — CLOSEOUT §8 교훈 4 의 새 사례

`[검증된 사실]` 직전 세션에서 `N-2b-1` 의 최초 구현이 공허했다. 두 겹이었다.

**(a) 위배 검사가 절대 참이 될 수 없었다** — `changed` 를 "값이 다르다" 로 정의하고
위배를 "값이 같다" 로 물었다. 위배 0 이 측정 결과가 아니라 **구성상 0** 이었다.

**(b) 집합 `S` 가 사전등록이 말한 것이 아니었다** — `|S| = 36,609`(구간당 ~1,900)는
정정 공시가 아니라 **회계기간이 FY→H1 로 넘어간 것**을 센 값이었다.

> **이것이 CLOSEOUT §8 교훈 4「같은 지문이 같은 원인을 뜻하지 않는다」의 새 사례다.**
> 회계기간 전환과 정정 공시는 **`amount` 가 달라진다는 같은 지문**을 남긴다.
> 지문만 보고 집합을 만들면 둘이 섞이고, 섞인 쪽이 압도적으로 크면 검사가 무의미해진다.
>
> **설계 결함은 하한만 검사한 것이다.** 사전등록 §5-3 은 `|S| = 0` 이면 공허하다고
> 적었지만 **상한을 두지 않았다.** `|S|` 가 유니버스 크기에 육박하면 그것도 공허하다 —
> "정정이 있는 종목" 이 전 종목일 리 없기 때문이다.
> **크기 자체가 신호였는데 1차 보고에서 통과로 읽었다.**
>
> `[Claude 의견]` 다음에 집합 기반 검사를 설계할 때는 **기대 규모의 상·하한을 함께**
> 적어라. "0이면 공허" 만으로는 부족하다.

정정 후: `S` = `available_from <= d` 且 `amendment_from > d` 인 자본총계 행.
`|S| = 198`, 룩어헤드 0, 미매칭 0.

### 1-4. `[정리]` §4-1 절대 허용오차 `0.05` 의 근거

A-2 의 교차확인(P-2 실측 검출률 vs 정규근사 예측)에 쓴 허용오차다.

`[검증된 사실]` R=1,000 회 반복에서 검출률 `p̂` 의 표준오차는 `√(p(1−p)/R)` 이고,
`p ≈ 0.6~0.9` 구간에서 최대 `≈ 0.0155`(p=0.5 일 때 0.0158)다. `0.05` 는 그 **3σ 남짓**이다.
근사가 √2 배쯤 어긋나는 진짜 오류(예: SE 차원 혼동)는 검출률을 0.2 이상 움직이므로
확실히 걸린다.

> **`a2_power.json` 에는 적지 않는다.** §6-1 화이트리스트가 최상위 키를 8개로 동결했고,
> 근거 한 줄을 넣자고 방화벽을 여는 것은 순서가 뒤바뀐 일이다. 근거는 상수가 있는
> `scripts/xsec/power.py` 와 이 기록에 둔다.

---

## §2. 결과

산출물: `experiments/analysis/2026.08.26._xsec_controls/{e1_estimate,diagnostics,n2b2_diagnostic}.json`.
선행 게이트 3종 통과 — `all_pass=true` / `ran == {N1,N2a,N2b1,P1,P2}` / §6-1 금지 키 부재.

### 2-1. E1 primary `[검증된 사실]`

L3ᴿ · T=19 (`SPEC15_T19_primary`, sha256 `7b1d7489522f8747…`) · 동일가중 · rank IC

| 항목 | 값 |
|---|---:|
| `mean(IC)` | **0.077202** |
| `sd(IC)` | 0.141445 |
| `ICIR` | 0.5458 |
| `t` (df=18) | 2.3791 |
| **permutation 단측 p** | **0.0000** (B=1,000, seed `2026082601`) |
| 부호 | **양(+)** = 가설 방향 |

구간별 IC (19개): `0.174, 0.125, −0.246, 0.195, 0.193, 0.045, −0.100, −0.127, 0.388,
0.167, 0.108, 0.151, 0.009, −0.008, −0.006, 0.173, 0.125, 0.053, 0.047`

**두 추론 경로가 자릿수 하나만큼 다르다** — 병기한다.

| 분모 | SE | 통계량 | 단측 p |
|---|---:|---:|---:|
| permutation 귀무 | 0.015359 | z = 5.027 | **0.0000** (P-1 기준) |
| 실현 `sd(IC)/√T` | 0.032450 | t = 2.3791 | **0.0143** (보수적) |

`[Claude 의견]` 차이의 원인은 **permutation 이 IC 시변성을 귀무에 넣지 않는 것**이다 —
v0.4 §6 이 `MDE_perm` 을 낙관적이라 부르고 `MDE_ICIR` 을 판정에 쓰기로 한 바로 그 이유다.
**P-1 의 사전등록 기준은 permutation p 이므로 PASS 다.** 보수적 읽기로도 0.05 미만이다.

### 2-2. 게이트 판정 (사전등록 §7-1 을 그대로 적용)

| 게이트 | 결과 | 근거 |
|---|---|---|
| **P-1** | **PASS** | permutation 단측 p `0.0000` < 0.05, 부호 양 |
| **P-2** | **FAIL** | FM λ̄ = **−0.005693** (IC 와 부호 반대), t=−0.6205, p=0.2770 |
| **P-3** | **PASS** | 전 조합 부호 `+` |

병기 — FM 통제 없음: λ̄ = **+0.004739**, t=0.4022, p=0.3670. 부호는 IC 와 일치하나 p 미달.

> `[Claude 의견]` **통제 3개를 넣으면 λ̄ 부호가 뒤집힌다.** 두 가지가 섞여 있다 —
> (a) 통제(`log_mktcap`·`mom_126`·`log_turnover`)가 신호를 흡수한다,
> (b) rank IC 와 선형 FM 은 다른 추정량이다(전자는 순위, 후자는 winsorize 후 선형).
> 통제 없는 FM 도 p=0.367 로 약한 것을 보면 (b) 의 몫이 작지 않다 — 순위 상관은 강한데
> 선형 회귀는 약하다는 것은 관계가 단조이되 선형이 아니라는 뜻이다.
> **통제는 동결이라 손대지 않았다 (C-1).** 어느 쪽 몫이 큰지는 이 설계로 가르지 못한다.

**T=18 병기** (G1 정합용, 판정 대체 아님): `mean(IC)=0.071827`, ICIR 0.5004,
t 2.1231, permutation 단측 p `0.0000`.

### 2-3. ⚠ 사전등록의 공백 — **결과를 본 뒤 고르지 않는다**

`[검증된 사실]` v0.4 §7-1 은 게이트 셋(P-1·P-2·P-3)을 나열하지만 **셋을 하나의
PASS/FAIL 로 합치는 규칙이 없다.** §7-3 조합표는 단일 PASS/FAIL 을 키로 쓴다.
**P-2 가 FAIL 이라 이 공백이 실제로 갈린다.**

| 읽기 | 근거 | §7-3 행 |
|---|---|---|
| `PASS = P-1` | 이 세션 착수 기록 §1-1 (커밋 `8aa5a55`, **결과 전**) | PASS 행 |
| `PASS = P-1 ∧ P-2 ∧ P-3` | v0.4 §7-1 의 "게이트" 어법 + SPEC_10 §5-1 선례("하드 게이트 — 전부 충족") | 중간 구간 행 |

**두 행의 다음 행동이 같다:**

> **PASS 행** — 「예측력은 있으나 **breadth 26으로는 포트폴리오 수준에서 검출되지
> 않는다**(§9-3). G1 FAIL과 모순 아님. 채택 **여전히 보류.** breadth 축이 후속 SPEC 대상」
>
> **중간 구간 행** — 「경제적 유의 수준(IR 0.5)은 배제됐으나 **문헌 수준(0.03~0.05)은
> 판정 불가.** 확립된 것은 *현행 breadth에서 채택 근거 없음*이지 신호 부재가 아니다.
> **breadth 축 SPEC이 선행되어야 하며, 그 전에는 1/PBR 폐기 결정을 내리지 않는다**」

`[Claude 의견]` 둘 다 **채택하지 말고, 폐기하지도 말고, breadth 축이 다음**이다.
**어느 쪽을 택해도 행동이 바뀌지 않으므로 결과를 본 내가 고르지 않는다.**
사용자 판단으로 남긴다. 다음 SPEC 이 이 공백을 메울 때는 **게이트 결합 규칙을 명시**하라.

### 2-4. §9-3 breadth 산술 — 같은 단위로 닫는다 `[검증된 사실]`

`[정정]` v0.4 §9-3 의 `≈25.5%` 는 **gross** 기준이었다. 실현 성과 환산이므로
**net 연율 변동성 `25.7143%`**(A-1 산출)를 쓴다.

```
IR ≈ IC × √breadth,  breadth = 13 × 2 = 26,  √26 = 5.099

관측 mean(IC) 0.0772 → IR 0.394 → 초과수익 10.12%p/yr
IC*_lit       0.0300 → IR 0.153 →          3.93%p/yr
MDE_ICIR      0.0807 → IR 0.411 →         10.58%p/yr
IC*_econ      0.0981 → IR 0.500 →         12.86%p/yr
A-1 MDE(단측)                     20.72%p(로그) / 23.02%p(CAGR 환산)
```

**관측 IC 가 함의하는 초과수익 10.12%p 는 A-1 MDE 의 절반이 안 된다.**
「IC 유의 + G1 FAIL」이 정합적이라는 §9-3 의 예고가 수치로 닫힌다.
관측 IC 는 `IC*_econ` 미만이고 `MDE_ICIR` 보다도 근소하게 작다.

> `[Claude 의견]` **이 환산은 근사다.** Grinold 의 `IR = α/ω` 에서 `ω` 는 **잔차(액티브)
> 리스크**인데 여기에 **총 변동성**을 대입했다. `IC*` 산출에는 변동성이 쓰이지 않으므로
> **판정은 무영향**이고, 근사는 위 표의 "초과수익" 열에만 걸린다.
> 추적오차를 진단으로 병기해야 하나 `U_pbr_path_ew` 대비 TE 는 이 세션에서 산출하지
> 않았다 — 다음 세션 항목이다.

### 2-5. `N-2b-2` (이월분, **판정 비사용**) `[검증된 사실]`

| | mean IC |
|---|---:|
| `IC(PIT)` | 0.075228 |
| `IC(future)` | **0.090557** |
| 차이 | **+0.015329** |

대조 구간 19개(공통 종목만). **방향은 `IC(future) > IC(PIT)` — v0.4 §5 가 "정상"이라
규정한 방향이다.** 미래 재무가 예측력을 더 준다.

> **교락 단서**: 역전·동일이어도 FAIL 이 아니다 — (a) PIT 파손과 (b) 미래 재무
> 무예측력을 IC 비교만으로는 가르지 못한다. 그래서 v0.4 가 값 수준(`N-2b-1`)을 primary 로
> 올렸고, 그것은 `S-3` 에서 이미 통과했다(|S|=198, 룩어헤드 0, 미매칭 0).
> **이 결과로 `N-2b-1` 판정을 뒤집지 않는다** (v0.4 §8 금지 9).
>
> `IC(PIT)` 0.075228 이 §2-1 의 0.077202 와 다른 것은 **공통 종목으로 제한**했기 때문이다.

### 2-6. 진단 병기 — 전부 **판정 비사용**

**계층별 IC**

| L0 | L1 | L2 | L3 | L3ᴿ |
|---:|---:|---:|---:|---:|
| +0.1338 | +0.1298 | +0.0761 | +0.0772 | +0.0772 |

`[Claude 의견]` **필터를 거칠수록 IC 가 줄어든다.** L0(+0.134) → L2(+0.076) 에서 반토막이다.
읽는 방법은 하나가 아니다 — 필터가 신호를 깎았을 수도, 필터가 이미 신호와 같은 방향으로
종목을 걸러 랭킹이 더할 여지를 줄였을 수도 있다. **v0.4 §2 가 지적한 교락이 이것이고,
이 설계는 둘을 가르지 못한다.** primary 는 L3ᴿ 하나다.

**유동성 3분위 IC**: q1(저) `+0.0198` / q2 `+0.0423` / q3(고) `+0.0509`.
신호가 저유동성에 몰려 있지 **않다.**
⚠ **이미 절단된 분포다** — HardFilter 가 L1 에서 일평균 거래대금 1억원 컷을 건다
(`hard_filter.py:32`). 컷 아래가 패널에 없으므로 "초소형주에서 IC 가 나오는가" 를
이 설계로는 온전히 답할 수 없다 (v0.4 §9-4).

**L3→L3ᴿ 제외**: 14행, **전부 `equity_nonpositive`**
(`mktcap_nonpositive`·`price_missing`·`pbr_nonpositive` 각 0).

**구간별 N_t** (L3ᴿ): min 65 / median 274 / max 511.

**월간 확장 — 미실행.** `[검증된 사실]` 월간 캘린더가 존재하지 않는다
(`CALENDAR_CHOICES = ('SEMIANNUAL', 'A', 'C')`). 캘린더 인프라 신설 + 패널 재생성이
필요하다. **P-1b 에 따라 판정 비사용 항목이므로 판정에는 영향이 없다.**

### 2-7. §9-1 긴장 — 표본 체제의 성질

> `IC*` 근방을 검출할 힘이 있는 유일한 구성은 **월간 확장(T≈120)** 인데,
> P-1b 가 그것을 판정 비사용으로 못박았다. P-1b 자체는 타당하다 — 신호 자기상관 때문에
> 유효 T 가 120 보다 훨씬 작고, 반기가 실제 전략의 캘린더다.
> **그럼에도 "판정력 있는 구성은 판정에 쓸 수 없다" 는 구조는 남는다.**
> 설계 결함이 아니라 **이 표본 체제의 성질**이다.

이번 세션에서 월간이 아예 미실행이라 이 긴장은 **해소되지 않은 채 그대로 이월된다.**

### 2-8. §9-2 섹터 통제 불가

`SECTOR-DATA` 미해결(`open_issues`, severity high). 저PBR 이 특정 업종에 몰려 있으면
관측 IC 는 **업종 베팅의 산물**일 수 있고 이 설계는 그것을 분리하지 못한다.
G5 낙폭 원인 규명에서도 같은 병목이었다. **§2-1 의 PASS 는 이 한계 위에 있다.**

### 2-9. 채택 경로가 열리지 않는다 `[검증된 사실]`

현재 게이트 상태: **G1 FAIL** (`e2c2b8d`, n=18 절단) · **G5 FAIL**
(일별 net MDD −57.8177%, 문턱 −45%, `af529ff`) · **G2 PASS**.

**SPEC_15 가 PASS 여도 §7-3 PASS 행에 "채택" 이 없다.** 보류 사유가 둘이다 —
**breadth**(수익 축)와 **MDD**(위험 축).

`[검증된 사실]` G5 낙폭 구간은 `2018-05-14` 고점 → `2020-03-19` 저점(COVID)이다.
`[Claude 의견]` 레짐 기인이라 **breadth 축 SPEC 이 해결할 수 있는 것이 아니다.**
종목 수를 늘려도 전 종목이 같은 저PBR 팩터에 물려 있으면 낙폭은 줄지 않는다
(`G5-MDD` 이슈: "종목 수 축으로는 풀리지 않는다").
**즉 breadth 축을 열어도 채택 경로는 여전히 닫혀 있다.**

### 2-10. P-0 준수 확인

`[검증된 사실]` SPEC_15 산출 경로는 `gate_results_*.json` 과 `docs/CANONICAL.md`
게이트 표를 **판정·산식 입력으로 쓰지 않았고, 갱신하지도 않았다.**
해석 문맥으로 G1 FAIL 을 인용했으며 그 출처는 `e2c2b8d` 분석 산출물이다
(게이트 표가 아니다). §2-9 의 G5·G2 인용도 `af529ff` 실행 기록이 출처다.

---

## §3. 다음에 이어받을 것

1. **§2-3 게이트 결합 규칙** — 사용자 판단. 어느 읽기든 다음 행동은 같다
2. **`SUMMARY-JSON-STALE`** (blocked) — `run_ablation` 이 등록 기준선 `periods.csv` 를
   덮어쓰는 문제 + `gate_analysis`·`make_canonical` 에 `period_set` 개념 부재
3. **`MOMENTUM-DRIFT-016600`** — 원인 미특정
4. **승격·`origin` push 결정** — 감사 모드 미해제 상태
5. **breadth 축 SPEC 검토 여부** — 단, §2-9 대로 **G5 를 풀지 못한다**
6. **`U_pbr_path_ew` 대비 추적오차** — §2-4 근사의 진단 병기용, 미산출
7. **월간 확장** — 캘린더 인프라가 선행돼야 한다 (§2-6)

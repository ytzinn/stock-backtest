# G5·G2 재산출 (섀도우 세대) — 실행 기록

> **이 파일은 날짜 붙은 실행 기록이다. 수치를 고쳐쓰지 않는다** (CLAUDE.md 문서 정정 규칙).
> 정정이 필요하면 상단에 ⚠️ 배너만 단다.
>
> **위치**: `SPEC_15 v0.4` §10 「선행 조건」. 이 세션이 끝나야 `A-0 + S-5a` 로 넘어간다.
> **선행 문서**: `docs/설계/SPEC_15_cross_sectional_estimator_v0.4.md` (사전등록 `c369401`),
> `docs/검토/2026.08.22._DATA_INTEGRITY_CLOSEOUT.md` §7
> **표기 규칙**: `[검증된 사실]` / `[Claude 의견]` / `[확실하지 않은 사실]`. **수치도 라벨 대상이다.**

---

## §1. 실행 전 확정 — **결과 산출 전 커밋**

> 이 절은 **결과를 보기 전에** 확정된 것이다. 이 커밋이 순서의 증거다.
> 아래 어떤 항목도 결과를 본 뒤에 바꾸지 않는다.

### 1-0. 기준선 선언 (`BASELINES.json` 4축)

| 축 | 값 |
|---|---|
| branch | `shadow/fs-div-fallback` |
| HEAD | `c369401` (SPEC_15 v0.4 사전등록) |
| db_port | **5436** (`shadow-fsdiv-5436`) |
| artifact_root | `/home/milmelmul/stock-backtest-shadow/experiments` |
| 등록부 기준선 | `shadow_20260819` |

`[검증된 사실]` 운영 DB(5433)는 **읽기도 하지 않는다.**

### 1-1. 구간 집합 — **G1과 동일한 n=18 절단**

세 게이트가 **같은 대상**을 서술해야 한다. G1(`e2c2b8d`)이 n=18이므로 G5·G2도 n=18이다.

- **판정**: n=18 절단 집합
- **진단 병기**: 전 완결 구간본(n=20). **판정에 쓰지 않는다.**
  두 값이 다르다고 유리한 쪽을 고르지 않는다.

`[Claude 의견]` 근거: G5(일별 MDD)는 NAV 연속성을 요구하고, G1의 n=18은 복리 연쇄
연속성 때문에 만들어진 집합이다. 두 제약이 같은 성질이라 집합도 같아야 한다.

`[검증된 사실]` **집합은 SSOT 재적용으로 산출했다** — 숫자를 옮겨 적지 않았다.
경로: `scripts/analysis/coverage_slice.{load_rule_coverage, selected_anchors, contiguous_tail}`
+ `scripts/analysis/period_truncation.load_f_periods(root=experiments/_baselines/shadow_20260819)`.

```
완결 구간 (n_stocks>0, next_date 가 앵커)        : 20
임계 탈락 (tau=50%, 규칙 {R1,R2,R5,R6})          : 2016-08-18   (R5·R6 판정가능 0%)
연속화로 추가 상실                                : 2016-04-05
채택 구간                                        : n=18,  2017-04-05 ~ 2026-04-03
```

### 1-2. tape은 **전 구간**으로 생성하고, 절단은 **판정 단계**에서 한다

tape을 18구간으로 잘라 만들면 그 밖의 구간은 **영구히 대조 불가**가 된다.
SPEC_15의 패널은 T=19(2016-04-05 포함)라 L-1a 대조에 그 구간이 필요하다.

- **생성**: 전 구간 (`export_portfolios` 기본 범위 2016-04-05 ~ 2026-04-03)
- **절단**: `run_daily_nav` 결과 슬라이싱 / G2 산출 단계

### 1-3. `period_set` 필드 신설

모든 게이트 산출물 JSON에 아래를 넣는다. `BASELINES.json` 스키마 가드와 같은 성격이다.

```json
"period_set": {
  "n": 18,
  "dates": ["2017-04-05", ...],
  "sha256": "<정렬된 날짜 목록을 LF 로 join 한 문자열의 sha256>",
  "source": "e2c2b8d 절단 집합"
}
```

**`[검증된 사실]` 이 세션이 쓰는 `period_set.sha256`** (산출 전 확정):

```
n      = 18
sha256 = d318d475e89c438dce62873a6c38d1b422c2d042c52cf99eadccb7797495d383
dates  = 2017-04-05 2017-08-18 2018-04-04 2018-08-20 2019-04-03 2019-08-20
         2020-04-03 2020-08-20 2021-04-05 2021-08-19 2022-04-05 2022-08-18
         2023-04-05 2023-08-18 2024-04-03 2024-08-20 2025-04-03 2025-08-20
해시 규약 = sha256("\n".join(sorted(dates)))   # 종료일(next_date)은 포함하지 않는다
```

`[Claude 의견]` 이 필드가 없어서 2026-08-12 오귀속(n=20 tape을 n=13 운영에 사용)과
08-24 세대 혼입이 조용히 통과했다. 필드가 있으면 이후 어떤 비교든 자동 대조된다.

### 1-4. 문턱 불변

| 게이트 | 기준 | 출처 | 이 세션에서 |
|---|---|---|---|
| **G5** | 일별 net MDD가 **−45%** 보다 얕음 | `scripts/robustness/gate_analysis.py:51` `G5_MDD_LIMIT = -0.45` (사전등록 2026-07-19) | **불변** |
| **G2** | F net CAGR **>** `U_pbr_path_ew` net CAGR | `docs/설계/SPEC_10_*.md` §5-1 | **불변** |

`[검증된 사실]` 두 값 모두 코드·설계서에서 읽어 온 것이며 이 세션이 정하지 않았다.

> `[Claude 의견]` 재산출은 **데이터 정정 반영**이지 기준 재설정이 아니다.
> G5는 FAIL이 예상되는 구간이라 재해석 유혹이 생기는 지점이다.
> **예상대로 나오는 것을 확인하는 것이 사전등록의 절반이다. FAIL은 실패가 아니라 결과다.**

### 1-5. `[산출 전 선언]` G2 벤치마크의 알려진 불일치 — **이 세션에서 고치지 않는다**

`[검증된 사실]` 사전등록 G2의 벤치마크 `U_pbr_path_ew` 는 모멘텀이 **MA 20/60** 이고
채택안은 **MA200** 이다. 즉 G2는 채택안을 **다른 유니버스의 동일가중**과 견준다.
이 불일치는 SPEC_10 §「미해결」에 이미 기록돼 있고 `dashboard/claims.KNOWN` 에 등록돼 있다.

**고치지 않는 이유**: 벤치마크 교체(`U_pbr_ma200_ew` 신설)는 **사전등록 게이트 정의의 변경**이며
별도 사용자 결정 사항이다. 이 세션의 임무는 **같은 정의를 정정된 데이터로 다시 재는 것**이다.

> **그러므로 이 세션의 G2 결과는 "채택안이 MA200 유니버스의 동일가중을 이겼는가"를
> 말하지 않는다.** 그 질문에 답하려면 별도 태그가 필요하다. 판정 문구에 이 한계를 병기한다.

---

## §2. 실행 계획 (T-1 ~ T-7)

| 단계 | 내용 | 상태 |
|---|---|---|
| T-1 | 이 착수 기록 **단독 커밋** | ← 이 커밋 |
| T-2 | 섀도우 n=13 tape 생성 (전 구간) | |
| T-3 | **세대 판별 양성 대조** + 전 구간 대조 | |
| T-4 | `period_set` 필드 도입 | |
| T-5 | G5 산출 (MDD 계산기 양성 대조 선행) | |
| T-6 | G2 산출 | |
| T-7 | 발행·`CANONICAL`·`open_issues`·`BASELINES` 갱신 | |

### 양성 대조 — **본 측정보다 먼저** (CLOSEOUT §8 교훈 1)

| id | 내용 | 통과 조건 |
|---|---|---|
| **PC-1 세대 판별** | 재생성 tape의 `2017-04-05` 등가중 구간 수익률 | 섀도우 `{tag}_periods.csv` 값과 일치. **운영 세대 값이 나오면 즉시 중단** |
| **PC-2 전 구간 대조** | 재생성 tape 구간 수익률 vs 섀도우 `periods.csv` | 허용오차 `5e-7` (tape `ret` 은 `round(...,6)`). **불일치 1건이라도 중단** |
| **PC-3 편입 집합** | 재생성 tape 편입 종목 집합 | **허용오차 없이** 일치 |
| **PC-4 MDD 계산기** | 합성 NAV(고점 대비 −30% 하락 후 회복) 주입 | MDD가 −30%를 돌려줘야 함 |

`[Claude 의견]` PC-1·PC-2의 기대값은 **이 문서에 적지 않는다.** 섀도우 `periods.csv` 에서
직접 읽어 대조한다 — 인용값을 기대치로 쓰면 그 인용이 틀렸을 때 대조가 통째로 무의미해진다.
(외부 리뷰가 제시한 `2017-04-05` 값들은 **`[확실하지 않은 사실]`** 로 취급한다.)

**PC-2·PC-3 실패 시**: 검출기를 느슨하게 하지 않는다. 이 계열에서 양성 대조 실패의
원인은 검출기가 아니라 **기대치 산술**이었다 (CLOSEOUT §8 교훈 2).

---

## §3. 이 세션이 하지 않는 것

1. 승격 (master 병합·감사 모드 해제) — **사람 판단 사항**
2. `git push` — 별도 지시 전까지 로컬 커밋만
3. 운영 DB(5433) 접속 — 읽기도 하지 않음
4. SPEC_15 본 측정 (`A-0` 이후)
5. 게이트 문턱·정의 변경 (§1-4, §1-5)
6. 기존 `F_pbr_ma200_n20_holdings.json` 덮어쓰기
7. 로컬 운영 세대 `F_pbr_ma200_n13_holdings.json` 을 참조물로 사용

> **P-0 은 이 세션에 적용되지 않는다.** P-0(게이트 표를 판정·산식 입력으로 쓰지 않음)은
> SPEC_15 전용 규칙이다. 이 세션은 게이트 표를 **갱신하는 것이 임무**다.

---

## §4. 결과

산출물: `experiments/analysis/2026.08.25._g5g2_reissue/{controls,tape,gates}.json`
+ `gate_results_g5g2.json`. 아래 수치는 전부 그 산출물에서 왔다.

### 4-0. 양성·음성 대조 — **본 측정보다 먼저 통과** `[검증된 사실]`

| id | 내용 | 결과 |
|---|---|---|
| 기준선 전수 대조 | `verify('shadow_20260819', …)` sha256 | tape 단계 3개 / gates 단계 4개 **통과**, `공식 수치 자격=true` |
| **검사기 음성 대조** | 일별 NAV csv 에 1바이트 추가 후 재실행 | `BaselineMismatch`, 종료코드 1, **산출물 미생성**. 원본 sha256 원복 확인 |
| **PC-1 세대 판별** | `2017-04-05` 등가중 (섀도우 csv 직접 읽음) | tape `0.0351199231` vs csv `0.0351197488`, `\|diff\|=1.743e-07` < `5e-7` **OK** |
| **PC-2 전 구간** | 21구간 수익률 대조 | 최대 `\|diff\|=1.743e-07` < `5e-7` **OK** |
| **PC-3 편입 집합** | n=20 tape 상위 13 과 대조 | 21구간, 불일치 **0** (허용오차 없음) **OK** |
| **PC-4 MDD 계산기** | 합성 −30% / 단조증가 / 문턱 ±0.01%p | 4건 **OK** (아래) |
| G-NAV-1/2/3 | `run_daily_nav` 내부 reconciliation | **PASS** (일별 NAV 가 엔진 구간수익률과 맞물림) |

PC-4 세부: 설계 −30% → `-0.30000000000000004` (peak `2020-01-01`, trough `2020-07-28`) /
단조증가 → `0.0` / −44.99% → PASS · −45.01% → FAIL (비교 방향 정상).

> `[Claude 의견]` PC-1 이 이 세션의 핵심 대조다. 운영(정정 전) 세대였다면 `0.059…` 가
> 나왔을 것이고, 그 tape 으로 G5 를 냈다면 08-24 혼입이 그대로 재현됐다.
> **음성 대조를 함께 둔 이유**는 양성만으로는 "항상 통과하는 검사기"를 못 걸러서다 —
> 실제로 1바이트 변조에 검사기가 물었고, 그래야 위 통과들이 의미를 갖는다.

### 4-1. 구간 집합 `[검증된 사실]`

사전등록 규칙 재적용 결과가 §1-3 에 박아 둔 값과 **정확히 일치**했다 (fail-closed 통과).

```
완결 20 → 임계 탈락 2016-08-18 → 연속화 상실 2016-04-05 → n=18
2017-04-05 ~ 2026-04-03
sha256 = d318d475e89c438dce62873a6c38d1b422c2d042c52cf99eadccb7797495d383
```

### 4-2. G5 — **FAIL** `[검증된 사실]`

| | 구간 | 거래일 | 일별 net MDD | 한계 | 판정 |
|---|---|---:|---:|---:|---|
| **판정** | n=18 `2017-04-05~2026-04-03` | 2,204 | **−57.8177%** | −45.00% | **FAIL** |
| 진단 (판정 비사용) | 전 완결 n=20 `2016-04-05~2026-04-03` | 2,452 | −57.8177% | — | — |

낙폭 구간: **고점 `2018-05-14` → 저점 `2020-03-19`** (COVID).

> `[검증된 사실]` **판정과 진단이 16자리까지 동일하다.** 우연이 아니라 낙폭 구간 전체가
> n=18 창 **안**에 있기 때문이다. 즉 이 게이트에서는 절단이 판정에 아무 영향이 없다.
> `[Claude 의견]` 그래서 "절단 덕에/탓에 FAIL" 이라는 해석은 성립하지 않는다.

### 4-3. G2 — **PASS** `[검증된 사실]`

| | F net CAGR | `U_pbr_path_ew` net CAGR | 마진 | 판정 |
|---|---:|---:|---:|---|
| **판정 n=18** | **14.0077%** | **7.2406%** | **+6.7672%p** | **PASS** |
| 진단 전구간 n=20 | 13.9757% | 5.0507% | +8.9250%p | 판정 비사용 |

> **§1-5 한계 병기**: 이 PASS 는 "채택안이 **MA200 유니버스의** 동일가중을 이겼다"를
> 뜻하지 **않는다.** 벤치마크 `U_pbr_path_ew` 의 모멘텀은 MA 20/60 이라 **다른 유니버스**의
> 동일가중과 견준 것이다. 사전등록 정의를 그대로 쓴 결과이며, 교체는 별도 결정 사항이다.

`[검증된 사실]` **부수 대조**: 진단 전구간 F net `13.9757%` 가 `BASELINES.json` 의
`shadow_20260819.expected_metrics.f_net_cagr = 0.139757` 과 공표 정밀도까지 일치한다.
등록된 기준선 지표를 독립 경로(구간 CSV → `compute_cagr`)로 재현한 셈이다.

`[Claude 의견]` `run_daily_nav` 로그의 net CAGR `13.8803%` 는 위 `13.9757%` 와 다르다.
**결함이 아니라 추정량 차이**다 — 전자는 일별 NAV 종점 기반(`compute_nav_cagr`),
후자는 구간 수익률 연쇄(`compute_cagr`)다. G5 는 일별 NAV(SSOT), G2 는 구간 지표를 쓴다
(SPEC_13 §9-1, 그리고 원래 G2 도 구간 지표였다). 두 게이트가 각자 맞는 SSOT 를 쓴 것이다.

### 4-4. 예상과 어긋난 것

`[Claude 의견]` **"없음"이 아니다.** 셋 있다.

1. **`SUMMARY-JSON-STALE` 이 `open_issues.yaml` 에 애초에 등재돼 있지 않았다.**
   SPEC_15 v0.4 §0-3 이 "별건 등재한다"고 적었으나 실제로는 되지 않은 상태였다.
   이 세션에서 등재했다 (`status: blocked`).

2. **워크트리 밖 실행 시 운영 DB(5433)로 조용히 붙는 경로를 발견했다.**
   `/tmp` 에 둔 스크립트는 `load_dotenv()` 가 섀도우 `.env` 를 찾지 못하고
   `get_connection()` 이 기본값으로 붙는다. assert 가 걸어 막았다.
   이후 모든 원격 실행에 **`price_history_recheck` 존재 여부**로 섀도우를 판별하는
   검사를 넣었다 — 포트 환경변수보다 강한 판별자다.
   `open_issues.yaml` 에 `DOTENV-CWD-SILENT-5433` 으로 등재.

3. **G2 마진이 절단으로 줄었다** (+8.93%p → +6.77%p). 원인은 F 가 아니라 **U 다** —
   벤치마크가 `5.05% → 7.24%` 로 올랐다. 즉 제외된 2구간은 채택안보다 **벤치마크를
   더 눌렀다.** 절단이 G1 에서는 합격선을 올렸고(e2c2b8d), G2 에서는 마진을 줄였다.
   방향이 같다 — 오염 구간이 **비교 대상 쪽에 더 유리하게** 작용하고 있었다는 뜻이다.

### 4-5. 하지 못한 것 — `docs/CANONICAL.md` **게이트 표 갱신 (blocked)**

`[검증된 사실]` CANONICAL 은 생성물이고 생성기가 읽는 입력 3종 중 **2종이 여전히 오염분**이다.

| 입력 | 상태 | 막힌 이유 |
|---|---|---|
| `daily_nav/summary.json` | **섀도우 세대로 대체됨** (이 세션) | — |
| `ablation/F_pbr_ma200_n13.json` | 오염분 | `run_ablation` 이 요약 JSON 을 낼 때 `{tag}_periods.csv` 를 **항상 함께 덮어쓴다**(`save_periods`, 건너뛸 플래그 없음). 그 파일은 `BASELINES.json` 에 `e82bdafd…` 로 등재된 **기준선 입력**이다 |
| `robustness/gate_results_*.json` | 오염분 | `gate_analysis.py` 는 **전 구간**에서 G1 을 스스로 다시 계산한다. 현행 G1(FAIL)은 `e2c2b8d` 가 n=18 에서 낸 것이라 그대로 돌리면 서로 모순된다 |

> `[Claude 의견]` 두 번째 행의 근인은 **생성기가 구간 집합(`period_set`) 개념을 갖고 있지
> 않다**는 것이다. 이 세션이 신설한 `period_set` 필드를 `gate_analysis`·`make_canonical`
> 까지 밀어야 풀린다. 첫 번째 행은 등록된 기준선 입력을 덮어쓰는 문제라 **사용자 승인
> 사항**이다 — 등록부가 막으려고 만들어진 바로 그 사고이기 때문에 임의로 하지 않았다.

그래서 CANONICAL 은 **미해결 과제 표에 위 두 항목을 추가하는 것까지만** 갱신했다
(생성기 재실행, 손편집 아님). 게이트 표·성적 표는 **손대지 않았다** — 여전히 오염분이며,
이제 그 사실이 CANONICAL 안에서 보인다.

---

## §5. 다음 세션이 이어받을 것

**SPEC_15 §10 선행 조건 충족 여부**: `A-0 + S-5a` **착수 가능**.

- ✅ G5·G2 재산출 완료 (섀도우, n=18)
- ✅ 섀도우 `F_pbr_ma200_n13_holdings.json` 생성 + `BASELINES.json` sha256 등재
  → SPEC_15 의 **L-1 배관 게이트 참조물 확보**. 전 구간(2016-04-05~) 생성이라
    SPEC_15 패널의 T=19 에 필요한 `2016-04-05` 구간도 들어 있다 (§1-2)
- ⬜ 승격 — **분리됨. 사람 판단 사항** (SPEC_15 v0.4 §0-2)

**남은 결정 사항 (사용자)**
1. `run_ablation` 재실행으로 `ablation/*.json` 을 섀도우 세대로 교체할 것인가
   — 등록된 `periods.csv` 를 덮어쓴다. 바이트 동일하면 무해하고 강한 대조가 되지만,
   달라지면 등록 기준선 위에 새 세대를 얹는 것이다
2. `gate_analysis`·`make_canonical` 에 `period_set` 을 도입할 것인가
   — 이것 없이는 CANONICAL 게이트 표가 절단 판정을 표현하지 못한다
3. 승격 시점
4. `git push` 시점 (감사 모드 미해제 상태라 이 세션은 로컬 커밋만 했다)
